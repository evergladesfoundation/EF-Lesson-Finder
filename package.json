{
  "name": "everglades-lesson-finder",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Everglades Literacy Lesson Finder — a Cloudflare Worker chat widget that searches the Master Lesson Index.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:key": "wrangler secret put API_KEY",
    "screenshots": "node tools/screenshots.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.55.0",
    "wrangler": "^4.28.0"
  }
}
