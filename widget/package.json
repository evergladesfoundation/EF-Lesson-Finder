{
  "name": "everglades-lesson-finder-widget",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && node scripts/search-smoke.mjs",
    "build": "vite build && cp demo/index.html dist/index.html",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
