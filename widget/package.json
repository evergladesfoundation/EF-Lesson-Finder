{
  "name": "everglades-lesson-finder-widget",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build && cp demo/index.html dist/index.html && node scripts/check-bundle.mjs",
    "preview": "vite preview",
    "test": "tsc --noEmit && vite build && cp demo/index.html dist/index.html && node scripts/check-bundle.mjs && node scripts/smoke-wix.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
