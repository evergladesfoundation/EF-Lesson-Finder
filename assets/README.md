# Assets

Widget screenshots used in the README and stakeholder walkthroughs live in `screenshots/`.

Regenerate them with the local Worker running:

```
npm run dev          # in one terminal, if it is not already up
npm run screenshots  # writes PNG files into screenshots/
```

The executive slide deck belongs in this folder when it is provided. Do not invent a placeholder PPTX or PDF.
